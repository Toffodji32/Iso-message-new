{% extends 'base.html.twig' %}

{% block title %}Groupe de Contacts{% endblock %}

{% block body %}
    <h1>Groupe de Contacts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contact_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contact_group_index') }}">Retour</a>

    <a href="{{ path('app_contact_group_edit', {'id': contact_group.id}) }}">Modifier</a>

    {{ include('contact_group/_delete_form.html.twig') }}
{% endblock %}
