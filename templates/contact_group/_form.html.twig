{{ form_start(form, {
    'action': action_url,
    'method': 'POST',
    'attr': {
        'id': 'contact-group-form',
        'class': 'needs-validation',
        'novalidate': 'novalidate'
    }
}) }}



<div style="margin-bottom: 20px;">
    {{ form_label(form.name, 'Nom du groupe', {'label_attr': {'style': 'display: block; margin-bottom: 5px; font-weight: bold;'}}) }}
    {{ form_widget(form.name, {'attr': {'style': 'width: 100%; padding: 8px; border-radius: 4px; border: 1px solid #ccc;'}}) }}
    {{ form_errors(form.name) }}
</div>

<div style="margin-bottom: 20px;">
    {{ form_label(form.contacts, 'Contacts', {'label_attr': {'style': 'display: block; margin-bottom: 5px; font-weight: bold;'}}) }}
    {{ form_widget(form.contacts, {'attr': {'style': 'width: 100%; padding: 8px; border-radius: 4px; border: 1px solid #ccc;'}}) }}
    {{ form_errors(form.contacts) }}
</div>

<div style="text-align: center;">
    <button type="submit" class="btn btn-primary">
        Enregistrer
    </button>
</div>


{{ form_end(form) }}
